{
  "name": "trello-copy",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "production": "rm -rf .next && next build && next start",
    "server": "node server.js",
    "server-production": "rm -rf .next && next build && NODE_ENV=production node server.js",
    "export": "rm -rf .next && rm -rf out && next build && next export",
    "export-production": "yarn run export && NODE_ENV=production node server.js",
    "test": "jest --watch"
  },
  "dependencies": {
    "express": "^4.17.1",
    "file-loader": "^5.1.0",
    "next": "^9.2.2",
    "prop-types": "^15.7.2",
    "react": "^16.13.0",
    "react-dom": "^16.13.0",
    "styled-components": "^5.0.1"
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^1.10.7",
    "enzyme": "^3.11.0",
    "enzyme-adapter-react-16": "^1.15.2",
    "jest": "^25.1.0",
    "jest-enzyme": "^7.1.2"
  }
}
